# Reflections | Projections Hype App

This app powers the hype site.

Use `apps/web/README.md` for workspace-level setup, scripts, and environment guidance.

From `apps/web`, run:

```bash
yarn workspace @rp/hype dev
```
