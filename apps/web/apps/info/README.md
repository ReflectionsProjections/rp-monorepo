# Reflections | Projections Info App

This app powers the info site.

Use `apps/web/README.md` for workspace-level setup, scripts, and environment guidance.

From `apps/web`, run:

```bash
yarn workspace @rp/info dev
```
