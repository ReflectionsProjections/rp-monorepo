# Reflections | Projections Admin App

This app powers the admin-facing web experience.

Use `apps/web/README.md` for workspace-level setup, scripts, and environment guidance.

From `apps/web`, run:

```bash
yarn workspace @rp/admin dev
```
