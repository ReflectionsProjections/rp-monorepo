services:
  rp:
    build: .
    container_name: rp_dev
    volumes:
      - ./dev:/shared  # Mount everything in shared folder
    ports:
      - "3000:3000"
      - "5173:5173"
      - "5174:5174"
    stdin_open: true   # Allows interactive shell
    tty: true          # Enables interactive mode